pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'build success'
            }
        }
          stage('Deploy') {
            steps {
                echo 'Deploy success'
            }
        }

      stage('Test') {
            steps {
                echo 'Test success'
            }
        }

      stage('Release') {
            steps {
                echo 'Release success'
            }
        }
      
    }
}
